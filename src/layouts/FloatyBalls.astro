---
import Base from './Base.astro';

const { title } = Astro.props;
---

<Base title={title}>
    <slot />
</Base>

<style is:global>
    .flexrow {
        display: flex;
        flex-direction: row;
        align-items: center;
        column-gap: 10px;
        margin-bottom: 6px;
    }
</style>
