---
const { title } = Astro.props
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="generator" content={Astro.generator} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="/particles/favicon.ico">
    <title>{title ?? "Particles"}</title>
</head>
<body>
    <slot />
</body>
</html>

<style is:global>
    * {
        box-sizing: border-box;
    }

    html, body {
        margin: 0;
        padding: 0;
    }

    button {
        border-radius: 4px;
        padding: 5px 10px;
    }

    button, input[type=color] {
        cursor: pointer;
        border: 0;
        box-shadow: rgba(0, 0, 0, 0.02) 0px 1px 3px 0px, rgba(27, 31, 35, 0.15) 0px 0px 0px 1px;
    }

    .pointer {
        cursor: pointer;
    }

    .flexrow {
        display: flex;
        flex-direction: row;
        align-items: center;
        column-gap: 10px;
        margin-bottom: 6px;
    }
</style>
